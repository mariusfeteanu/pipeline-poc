
```bash
sudo snap install jq
sudo apt install unzip
sudo apt  install libxml2-utils
```

```bash
. ./run.sh
```


curl -sL -A "Mozilla/5.0 (compatible; MyArxivBot/0.1; +https://example.com/bot)" \
  "http://export.arxiv.org/api/query?search_query=cat:math.NT&start=0&max_results=5"
